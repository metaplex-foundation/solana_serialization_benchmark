/**
 * This code was AUTOGENERATED using the kinobi library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun kinobi to update it.
 *
 * @see https://github.com/metaplex-foundation/kinobi
 */

import {
  Context,
  Pda,
  PublicKey,
  TransactionBuilder,
  transactionBuilder,
} from '@metaplex-foundation/umi';
import {
  Serializer,
  mapSerializer,
  struct,
  u8,
} from '@metaplex-foundation/umi/serializers';
import {
  ResolvedAccount,
  ResolvedAccountsWithIndices,
  getAccountMetasAndSigners,
} from '../shared';

// Accounts.
export type ReadCollectionBitcodeInstructionAccounts = {
  /** The address of the new account */
  address: PublicKey | Pda;
};

// Data.
export type ReadCollectionBitcodeInstructionData = { discriminator: number };

export type ReadCollectionBitcodeInstructionDataArgs = {};

export function getReadCollectionBitcodeInstructionDataSerializer(): Serializer<
  ReadCollectionBitcodeInstructionDataArgs,
  ReadCollectionBitcodeInstructionData
> {
  return mapSerializer<
    ReadCollectionBitcodeInstructionDataArgs,
    any,
    ReadCollectionBitcodeInstructionData
  >(
    struct<ReadCollectionBitcodeInstructionData>([['discriminator', u8()]], {
      description: 'ReadCollectionBitcodeInstructionData',
    }),
    (value) => ({ ...value, discriminator: 28 })
  ) as Serializer<
    ReadCollectionBitcodeInstructionDataArgs,
    ReadCollectionBitcodeInstructionData
  >;
}

// Instruction.
export function readCollectionBitcode(
  context: Pick<Context, 'programs'>,
  input: ReadCollectionBitcodeInstructionAccounts
): TransactionBuilder {
  // Program ID.
  const programId = context.programs.getPublicKey(
    'solanaSerializationBenchmark',
    'BENCHVr3SC7dVDMtKVpwctjFNPBMrqvXn9JVACJg3KEb'
  );

  // Accounts.
  const resolvedAccounts = {
    address: {
      index: 0,
      isWritable: false as boolean,
      value: input.address ?? null,
    },
  } satisfies ResolvedAccountsWithIndices;

  // Accounts in order.
  const orderedAccounts: ResolvedAccount[] = Object.values(
    resolvedAccounts
  ).sort((a, b) => a.index - b.index);

  // Keys and Signers.
  const [keys, signers] = getAccountMetasAndSigners(
    orderedAccounts,
    'programId',
    programId
  );

  // Data.
  const data = getReadCollectionBitcodeInstructionDataSerializer().serialize(
    {}
  );

  // Bytes Created On Chain.
  const bytesCreatedOnChain = 0;

  return transactionBuilder([
    { instruction: { keys, programId, data }, signers, bytesCreatedOnChain },
  ]);
}
